# Contributor Backlog

## Open issues for contributors

1. Add Postgres support (optional)
- Keep SQLite default for showcase mode.
- Add Postgres schema + migration path and docs.

2. Add additional human approval channels
- Slack approval bot.
- Microsoft Teams approval bot.
- Generic webhook approval sink.

3. Approval persistence
- Persist approval requests in database instead of in-memory Map.
- Add expiry, dedupe, and audit lifecycle states.
